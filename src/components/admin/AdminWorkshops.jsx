import React from "react";
import AdminEvents from "./AdminEvents";

/** Workshops tab — the same management surface as Tournaments. */
export default function AdminWorkshops() {
  return <AdminEvents kind="workshop" />;
}
